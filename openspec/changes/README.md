# OpenSpec Active Changes

Active OpenSpec changes live under this directory.

Before creating or modifying a change, read:

1. `AGENTS.md`
2. `_myplan_/planning-manifest.yaml`
3. `_myplan_/change-registry.yaml`
4. the planning sources registered for the target Change

The next planned change is:

```text
001-ai-company-organization-foundation
```

Use the installed OpenSpec CLI/workflow to generate and validate change artifacts rather than inventing an incompatible layout manually.
